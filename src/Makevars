MAC_CODE=Apple_Terminal
GCC_FLAGS=HAHAHAHAHAHAHA
CLANG_FLAGS=-std=c++0x
CLANG_FLAGS+=-stdlib=libc++
ifeq "$(TERM_PROGRAM)" "$(MAC_CODE)"
PKG_CXXFLAGS+=$(CLANG_FLAGS)
else
PKG_CXXFLAGS+=$(GCC_FLAGS)
endif
